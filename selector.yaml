all_agents:
  selector:
    location: nope
  policies:
    default:
      path: default.yaml
lab_agents:
  selector:
    location: lab
  policies:
    network_policy_1:
      enabled: true
      path: lab_agents/network.yaml
    device_policy_1:
      enabled: false
      path: lab_agents/device.yaml 
    demo_data_worker_1:
      enabled: true
      path: lab_agents/worker.yaml
    juniper_mist_worker_1:
      enabled: false
      path: lab_agents/juniper_mist.yaml
