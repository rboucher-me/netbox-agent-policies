all_agents:
  selector:
    location: nope
  policies:
    default:
      path: default.yaml
lab_agents:
  selector:
    location: lab
  policies:
    network:
      enabled: true
      path: lab_agents/network.yaml
    device:
      enabled: false
      path: lab_agents/device.yaml 
    worker:
      enabled: true
      path: lab_agents/worker.yaml
