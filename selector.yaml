webinar_1:
  selector:
    agent: demo-agent
  policies:
    network_discovery:
      path: network_discovery.yaml
webinar_2:
  selector:
    agent: none
  policies:
    device_discovery:
      path: device_discovery.yaml
webinar_3:
  selector:
    agent: none
  policies:
    worker_discovery:
      path: worker_discovery.yaml
