    worker:
      mist_policy:
        config:
          package: nbl_mist
          schedule: "* * * * *"
          mist_apitoken: ${MIST_APITOKEN}
          mist_org_id: ${MIST_ORG_ID}
        scope:
          site: all
